package main;

	import dao.ConexionBD;
	import java.sql.Connection;

	public class TestConexion {
/*
	    public static void main(String[] args) {
	        // 1. Creamos el objeto de nuestra clase de conexión
	        ConexionBD bd = new ConexionBD();

	        System.out.println("Iniciando prueba de conexión...");

	        // 2. Intentamos conectar
	        // Guardamos el resultado en una variable tipo Connection por si la necesitáramos
	        Connection con = bd.conectar();

	        // 3. Verificamos si la conexión tuvo éxito
	        if (con != null) {
	            System.out.println("¡Genial! El puente con la base de datos está abierto.");
	            
	            // 4. Una vez probado, cerramos la conexión
	            bd.desconectar();
	        } else {
	            System.err.println("¡Ups! Algo falló. Revisa que MySQL esté encendido en XAMPP.");
	        }    
	    }
	}
*/
	}
